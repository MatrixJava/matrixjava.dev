{
  "name": "terminal-portfolio-bun",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --hot server.ts",
    "start": "bun server.ts",
    "build": "bun run scripts/build.ts",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "bun-types": "^1.3.0",
    "typescript": "^5.9.0"
  }
}
